//entre com o nome e só imprima o nome se a primeira letra do nome for 'A'
#include <stdio.h>
int main(){
char nome [10];
    gets(nome);

   if  (nome[0]=='A'||'a'){
    printf("%s",nome);
        }else{
        }

return 0;}
